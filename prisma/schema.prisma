datasource db {
  provider = "postgresql"
  url      = env("postgresql://neondb_owner:npg_wj1JvXrdcs8a@ep-dry-union-a2jzx3wk-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require")
}

generator client {
  provider = "prisma-client-js"
}

model Leaderboard {
  id        Int      @id @default(autoincrement())
  username  String
  score     Int
  date      DateTime @default(now())
}
